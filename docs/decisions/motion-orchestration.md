# Recorded motion orchestration

Status: implemented on `feat/motion-orchestration`; comparative human evaluation pending.

The [quality strategy](motion-quality-strategy.md) sets the current priority: restore faithful full-body/finger replay from the original recordings, then review Idle, listening, conversation and their handoffs. Animates' music-synchronized dancing is a quality reference, not the first required feature. Current runtime improvements are foundations, not proof that the material or product meets that reference.

Yorishiro now uses recorded VRMA motion as the main idle and conversational body performance. Procedural breathing, gaze and small offsets supplement the recording. Previously those offsets overwrote the spine and arm rotations even when a recorded clip had most of the weight; that mixing error is fixed.

## Runtime

`Body` warms nine local recordings from the existing asset store. `MotionDirector` queries an inspectable six-dimensional semantic index (calmness, attention, reflection, affiliation, emphasis, movement energy), filters context and cooldown, and samples at most five eligible candidates. This is a small local vector search, not a language-model embedding or an external vector database. No paid API, local model download or extra inference is needed. Existing assistant transcript interpretation supplies speech intent; optional inline text tags are unnecessary. The shared `VoicePlayer.say(text)` path also passes text through the same local resolver, so MCP speech, Quick Chat and persona speech using the existing OS `SayTtsEngine` can drive motion without a realtime API. Cues remain pending during synthesis and acquire ownership only after Web Audio starts. Stop, cancellation and playback-owner changes release them; the unclocked native fallback does not pretend to provide gesture timing.

Idle motifs normally last 12–25 seconds with occasional extra quiet time. Immediate repetition is excluded; recent clips and motion families are penalized. Listening narrows the eligible pool to quiet attentive recordings. Thinking, speaking, interruption, ownership and reduced motion each have explicit behavior. A voice utterance owns its gesture; a nearby facial cue does not restart or truncate it when gesture selection is temporarily suppressed. After a finite gesture, the recorded idle can resume after 2.5 seconds of settling. Leaving audible speech releases the conversational background even if every attentive replacement is missing, cooling down or physically incompatible.

Neutral speech also has a recorded performance. Actual audio start selects an explanatory background from three conversational recordings, with 10–18 second dwell times and weight 0.85 before the user's intensity setting. This does not require an emotional keyword. The earlier approximately 0.4 weights visibly weakened authored hand poses; [target-avatar gain review](conversation-performance-gain-review.md) records the comparison and its limits. Chatting 2 is classified as animated conversation, with higher energy and emphasis, rather than quiet reflection. Facial-cue expiry releases only the face; finite body gestures use their authored end, or a low-velocity exit 6–6.5 seconds after entry for longer recordings, followed by their fade. Audio completion and interruption still release speech ownership. Grounded speaking also remains eligible during simultaneous reading/writing/running tool activity, while explicit motion owners and claims keep priority. Once conversation phases are supplied, they take precedence over a lip-sync analyser that stays active for an entire realtime connection; listening and silent idle therefore resume while the connection remains open.

`AnimationPlayer` retargets and analyzes quaternion poses and angular velocities once per clip variant, with bounded sampling. Before the weighted draw, automatic selection compares eligible candidates with the actual mixed upper-body pose and signed angular velocity at the requested contribution. Incompatible or unprepared candidates are excluded; transition cost adjusts the remaining sampling weights before the final top five. The chosen incoming phase is passed to playback. Authored finite gestures retain their beginning. Explicit matched playback without a selected entry can also search a short outgoing low-speed window. Incoming and outgoing weights follow smoothstep ramps without changing playback speed to fit unrelated clip lengths. Asynchronous loading and delayed entry remain conditional on current ownership. Old fades are retired when an external animation owner takes over.

Looping recordings receive a cached tail correction. The final 0.4–0.8 seconds converge to the first pose and its angular velocity using a quintic envelope. Original non-looping performances stay intact. This closes the internal loop seam as well as the seam between different clips. It is a bounded alteration of the end of a looping recording, not generated motion or an IK solver.

## Contact and scope

Automatic performances use an upper-body bone mask over an independent recorded standing foundation. The reviewed `Idle` clip supplies hips, legs, ankles and toes continuously, while the selected recordings supply torso, arms, head and fingers. Replacing an upper-body performance does not restart the lower-body cycle. Explicit full-body performances and animation claims take precedence over the foundation. Whole-character breathing displacement is attenuated by recorded weight on either layer.

The standing foundation first transfers Idle's small recorded rotation changes relative to its first frame onto the target's normalized rest pose. This avoids a 10–16 cm foot displacement caused by blending directly into the source's different initial stance. After loop conditioning, target-skeleton forward kinematics recovers the shared displacement of both ankles and toes and adds the inverse to the hips position. It does not use the source translation scale: the converter's zero rest hip height makes that scale invalid. This preparation runs once on private transform copies, without posing the live avatar.

This restriction is deliberate: the [29-asset audit](motion-assets-analysis.json) found that removing hip translation from several existing recordings changes foot motion substantially. Low-speed frames can still differ in foot placement by tens of centimetres. Foundation calibration and grounding apply only to the reviewed quiet Idle, whose lower-body changes stay within about 1.12 degrees. They do **not** provide arbitrary full-body contact-aware motion matching, foot IK, locomotion, floor collision or contact inference. Handoffs from an arbitrary explicit leg stance can still move the feet; those require separate transition/contact work.

The default library is five idle recordings and four conversational recordings. Selection, phase matching and loop correction remain separate operations. The [candidate compatibility audit](motion-candidate-compatibility-metrics.json) checks the new upper-body rotation/velocity gate; it does not measure full-body contacts or certify authored gesture boundaries. The source assets remain in `../Yorishiro-assets`, following existing distribution and licensing rules.

The [standing measurements](standing-idle-grounding-metrics.json) replay the real mixer at 120 Hz on Yori, including SDK-updated raw bones. Across two loops the calibrated, grounded foundation's maximum foot drift is 0 / 0.259 / 0.645 / 1.286 mm at weights 0 / 0.2 / 0.5 / 1. The 1.2-second start fade stays within 1.286 mm, compared with 161.302 mm for an uncalibrated absolute-pose fade; fading out and reducing gain from 1 to 0.2 stay within 0.299 / 0.328 mm respectively. These tests use phase-zero starts and no procedural layers; they establish the reviewed foundation's behavior, not universal foot locking or human preference.

## Use and verification

Run from the worktree:

```sh
npm run motion:lab
npm run motion:audit
npm run motion:seams
npm run motion:grounding
```

The lab is at `http://127.0.0.1:1437/motion-lab.html`. It compares the same Yori avatar, camera and lighting with the library disabled/enabled and lets a reviewer exercise semantic gestures. It is a development entry point, not part of the release bundle. “Export observations” saves selected clips, timing and sampled head/hand positions. `scripts/capture-motion-lab.mjs` can automate screenshots and, with `--film`, a 30-second comparison video using a separately available Playwright/browser and ffmpeg. `YORISHIRO_PLAYWRIGHT_MODULE`, `YORISHIRO_CHROME_PATH`, `YORISHIRO_MOTION_LAB_URL` and `YORISHIRO_MOTION_QA_DIR` configure those local tools and outputs. The default output is the Git-ignored `.motion-review/` directory.

For audible review, run `node scripts/prepare-motion-speech-sample.mjs` once on macOS, then click “Play local speech sample”. The generator requires the locally installed Japanese Kyoko voice and writes a 24.052-second WAV plus text manifest under `.motion-review/speech-sample/` without playing sound or downloading a voice. Both lanes receive the same VoicePlayer audio, mouth sample and expression cues through the existing runtime bridge. No microphone or autoplay is used. Cues begin on the actual playback clock; phrase offsets remain estimates from the existing resolver, not word alignment. Stop, interruption, pausing and changing playback mode release the sample. A browser frame gap above 250 ms also stops it instead of replaying delayed gestures against advanced audio.

`motionIntensity` scales automatic clip strength; zero releases automatic motion. `Body.setMotionLibraryEnabled(false)` allows a controlled baseline or an internal opt-out without cancelling a persona/MCP-owned performance. Advanced callers can set `MotionOptions.mask`, `transition` and `maxTransitionDelayMs` through the existing motion slot API. Semantic requests are currently an internal Body API.

Reviewed full-body one-shots can explicitly request `rootMotion: "preserve"` with immediate entry and looping disabled. The default remains in-place. This opt-in retains a validated finite hips XYZ track with the source-to-target rest-height scale; invalid source geometry or unsupported masked/matched/looping requests fail rather than silently losing translation. It does not enable the new conversion candidates in the automatic catalog. Their source fidelity, target adaptation and acting quality still require separate review.

Tests cover retrieval/diversity, real-mixer fade envelopes, phase selection, loop position/velocity continuity, ownership races, same-clip replay, claims, missing assets, conversational boundaries and reduced motion. The asset audit additionally checks its FK against the official retargeted Three.js mixer. The [loop metrics](motion-loop-seam-metrics.json) quantify improvement over the original source clip at its boundary; they are not measurements of Animates or human preference.

## Acceptance

Implementation and regression checks are distinct from the requested acceptance criterion. A comparison against installed Animates is still required to establish a clear advantage in naturalness and presence. Follow the [evaluation protocol and observed Animates conditions](motion-orchestration-evaluation.md). More motion variety or a lower boundary discontinuity alone does not establish that advantage.

## Verification record, 2026-09-14

The final source at `22720746`, including physical candidate selection, the 0.85 conversational gain, speech-boundary fixes, local OS speech bridge, recorded standing foundation, prototype exclusion, explicit root-translation preservation and optional speech lab, passed **3,047 tests across 241 frontend test files**. `npm run build` passed TypeScript checking and the production build; Biome passed all 47 changed TS/TSX/MJS/JS files since `2af32786`. The speech checks cover actual Web Audio start notifications through the OS synthesis adapter, overlapping requests, cancellation, playback-owner changes and the unclocked fallback. Logs are `.motion-review/final-speech-{vitest,build,biome}.log`. The build retains its large-chunk warning (the main App chunk is approximately 4,550 kB before gzip); jsdom tests retain canvas-not-implemented diagnostics.

Fresh browser QA used an isolated localhost server on port 1440 with HMR and file watching disabled. It exercised 232 simulated seconds through idle, gesture requests, a 45-second neutral explanation, and listening without page errors, and rendered a new 30-second, 720-frame, 1400 × 1000 comparison film at 24 fps. Artifacts and validation logs are separate from earlier runs under `.motion-review/orchestration-v3/`. `ffprobe` confirmed the encoded duration and frame count. The film driver checks animation time against video time; its 24 Hz capture steps preserve partial 60 Hz simulation steps. Runtime facial expression, hair and other VRM updates run once per frame.

The 232-second browser run sampled normalized foot markers with all Body layers active. From 3 seconds onward, the largest distance from the first sampled foot position was 1.496 mm for the recorded system and 12.286 mm for the procedural baseline. During the 45-second explanatory phase, actual selected clips included Conversation, Chatting and Chatting 2. At the listening boundary the speech background stopped, and an attentive Idle began about 0.6 seconds later. Gesture requests may be declined by the physical gate; a screenshot named for a requested intent is not proof that the gesture was admitted. These are state, clock and marker-motion diagnostics, not mesh-floor contact, speech-alignment or perceptual scores. This run and its film supply phases directly without spoken audio and predate the optional speech UI and root-preservation opt-in; automatic catalog playback is unchanged by those additions. The film uses the current nine-clip catalog and supersedes the earlier prototype-containing film for implementation review.

Separate real-audio QA on an isolated localhost server used the 24.052-second Kyoko file with Chrome output muted, while retaining the actual Web Audio clock and analyser. Across 24 samples, both lanes shared mouth values and expression cues, the recorded lane played conversational motion, and natural audio completion released to idle. Replay followed by interruption cleared the mouth and queued cues; page errors were empty. Under approximately 100 ms animation-frame spacing, 1.6069 seconds of wall time advanced Body by 1.5749 seconds (a 32 ms sampling-boundary difference), without the old silent-mode time clamp. A 300 ms frame delay stopped audio and cleared mouth values. Screenshots at 4 and 11 seconds were inspected. Observations are under `.motion-review/speech-sample/qa/`; this establishes playback integration, not perceptual speech alignment or acting approval.

The explicit root-preservation route was also compared with direct official-loader playback of the same prepared source and contact-adapted clips on Yori. Seek and continuous replay at 0, 3, 8, 15 and 25 seconds produced 60 paired hips/feet coordinate checks with zero recorded difference between lanes. Hips moved up to 41.626 mm for the source candidate and 37.751 mm for the adapted candidate, showing translation was retained. No page/resource errors occurred; the loader emitted its look-at proxy creation warning. `.motion-review/runtime-root-preservation-qa.json` records the setup. This isolated full-weight player check excludes Body overlays, automatic selection, transitions and perceptual quality.

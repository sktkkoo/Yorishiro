/**
 * PersonaRegistry — Active persona set を保持し、Persona Pack の dynamic import と per-pack bound PersonaContext instantiation を担う
 *
 * Philosophy: docs/INHABITED_INTERFACE_PHILOSOPHY.md「多人格の住人」+「自己生成 loop」
 * SDK surface: src/sdk/persona.d.ts の PersonaDefinition + src/sdk/context.d.ts の PersonaContext（27–108）
 *
 * 本 skeleton は Phase 3.3(g.1) で配置。real 実装は g.4 で TDD 予定。
 */
export class PersonaRegistry {}

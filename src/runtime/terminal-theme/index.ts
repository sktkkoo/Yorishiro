/**
 * TerminalTheme wire barrel — scene 切替時のターミナルカラー適用。
 */

export {
  getCurrentTerminalTheme,
  type InitTerminalThemeResult,
  initTerminalTheme,
  resolveTerminalTheme,
  syncCurrentTerminalTheme,
} from "./wire";

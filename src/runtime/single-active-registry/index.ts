/**
 * SingleActiveRegistry barrel — single-active pack 用 generic primitive。
 *
 * 詳細は ./single-active-registry.ts の docstring を参照。
 */

export { SingleActiveRegistry } from "./single-active-registry";
export type {
  Disposable,
  PackOrigin,
  SingleActiveEntry,
  SingleActiveRegistryOptions,
} from "./types";

/**
 * Expression — 表現 primitive。反射 handler の output を body / voice / space に route する層
 *
 * Philosophy: docs/philosophy/PRESENCE_HARNESS.md「六要素 > 表現」+「確率的表現」
 * SDK surface: context output APIs（CharacterAPI / VoiceAPI / SpaceAPI / RendererAPI）を束ねる internal
 *
 * 本 skeleton は Phase 3.3(g.1) で配置。real 実装は Phase 3.5 で TDD 予定。
 */
export class Expression {}

/**
 * Body — 身体 primitive。VRM character と voice（pre-recorded + 後日 TTS）を束ねる actuator 層
 *
 * Philosophy: docs/PRESENCE_HARNESS.md「六要素 > 身体」
 * SDK surface: src/sdk/context.d.ts の CharacterAPI（268–296）+ VoiceAPI（370–397）
 *
 * 本 skeleton は Phase 3.3(g.1) で配置。real 実装は Phase 3.5 で TDD 予定。
 */
export class Body {}

/**
 * Time — 独立した時計 primitive。now / after / every / atProbability / schedule + Cancellable を提供する
 *
 * Philosophy: docs/INHABITED_INTERFACE_PHILOSOPHY.md「独立した時間」+「適用範囲の補足」
 * SDK surface: src/sdk/context.d.ts の Time interface（214–260）
 *
 * 本 skeleton は Phase 3.3(g.1) で配置。real 実装は g.2 で TDD 予定。
 */
export class Time {}
